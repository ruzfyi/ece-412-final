# ECE-412 Final Project

**Requirements**
- `avr-gcc` for compilation
- `avrdude` for programming the device

**Install**
```sh
# Fedora
dnf install -y avr-gcc avrdude

# Debian
apt install -y avr-gcc avrdude
```
